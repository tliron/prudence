<!DOCTYPE html>
<html>

<%# insert the contents of a file %>
<%+ 'fragments/head.html' %>

<body>

<%# we'll automatically create a weak signautre based on the following section %>
<% context.startSignature(); context.weakSignature = true; %>

    <h1>
        Chores for: <%= context.variables.name %>
        <% if (context.query.scope === 'all') { %>(all of them)<% } %>
    </h1>

<% context.endSignature(); %>

<%# embed another cacheable representation (and optionally set a custom cache key) %>
<%& 'fragments/list.jst', 'list:' + context.variables.name %>

</body>
</html>

<% context.cacheDuration = 5; // in seconds %>
