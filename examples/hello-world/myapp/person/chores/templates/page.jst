<!DOCTYPE html>
<%# render a section (we *must* end the section later) %>
<%^ 'minhtml' %>
<html>

<%# insert the contents of a file %>
<%# by default all scriptlets (that are not '=') will skip a trailing newline after the end delimiter %>
<%# but if we put a slash just before it the newline will remain %>
<%+ '../fragments/head.html' /%>

<body>
    <%= context.variables.body %>
</body>

</html>
<%^^%>
